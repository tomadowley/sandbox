// import React from "react";
// import { render, screen } from "@testing-library/react";
// import App from "./App";

// NOTE: The original test is disabled because the app now renders a 3D simulation
// that relies on WebGL and react-three-fiber, which cannot be tested in jsdom.
// If you want to re-enable tests, consider mocking GorillaSimulator or writing non-3D logic tests.

// test("renders learn react link", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
//   expect(false).toBe(true);
// });
